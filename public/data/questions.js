/**
 * Questions
 */

var questions = module.exports = [];

questions.push({
  text: 'What is the output?'
, body: [
    '<p>Question body here answers somewhere else</p>'
  ].join('\n')
});

questions.push({
  text: 'What is the output?'
, body: [
    '<p>Question body here answers somewhere else</p>'
  ].join('\n')
});

questions.push({
  text: 'What is the output?'
, body: [
    '<p>Question body here answers somewhere else</p>'
  ].join('\n')
});

questions.push({
  text: 'What is the output?'
, body: [
    '<p>Question body here answers somewhere else</p>'
  ].join('\n')
});

questions.push({
  text: 'What is the output?'
, body: [
    '<p>Question body here answers somewhere else</p>'
  ].join('\n')
});

questions.push({
  text: 'What is the output?'
, body: [
    '<p>Question body here answers somewhere else</p>'
  ].join('\n')
});

questions.push({
  text: 'What is the output?'
, body: [
    '<p>Question body here answers somewhere else</p>'
  ].join('\n')
});

questions.push({
  text: 'What is the output?'
, body: [
    '<p>Question body here answers somewhere else</p>'
  ].join('\n')
});

questions.push({
  text: 'What is the output?'
, body: [
    '<p>Question body here answers somewhere else</p>'
  ].join('\n')
});